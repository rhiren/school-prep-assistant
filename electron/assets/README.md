Place the real macOS app icon at `electron/assets/icon.icns`.

This repository includes a placeholder `icon.icns` path so the Electron packaging setup is complete, but the actual `.icns` file should be provided by the developer before creating a final distributable app bundle.

Replace icon.icns with a real macOS icon before final packaging.
